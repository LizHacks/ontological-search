# Ontology-ingest
---
## Requirements
- pronto (`pip3 install pronto`)
- PyArango (`pip3 install pyarango`)
To run:
```
  python3 -m venv .
  source bin/activate
  python3 src/main.py <path-to-.owl-file>
```
